PORT=3080
HOST=127.0.0.1
# First boot only. At least 12 characters. No default password.
ADMIN_PASSWORD=
# 64 hexadecimal characters. Keep stable and back up separately.
ENCRYPTION_KEY=
# Set true behind HTTPS in production.
COOKIE_SECURE=false
